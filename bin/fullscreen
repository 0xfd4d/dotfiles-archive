#!/bin/sh

FSFILE=${FSFILE:-~/.fwin}

if test -f $FSFILE; then
    wtp $(cat $FSFILE)
    rm -f $FSFILE
    chwb -s 2 $1
else
    chwb -s 0 $1
    wattr xywhi $1 > $FSFILE
    wtp $(wattr xywh `lsw -r`) $1
fi

pfw
